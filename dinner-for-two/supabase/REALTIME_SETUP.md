# Настройка Supabase Realtime

Для работы WebSocket-подписок в реальном времени нужно включить Realtime для таблиц в Supabase Dashboard.

## Шаги настройки:

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **Database** → **Replication**
4. Включите Realtime для следующих таблиц:
   - ✅ `dishes`
   - ✅ `ingredients`

## Альтернативный способ (через SQL):

Выполните в SQL Editor:

```sql
-- Enable Realtime for dishes table
ALTER PUBLICATION supabase_realtime ADD TABLE dishes;

-- Enable Realtime for ingredients table
ALTER PUBLICATION supabase_realtime ADD TABLE ingredients;
```

## Проверка работы:

1. Откройте приложение в двух разных окнах/устройствах
2. Добавьте блюдо в одном окне
3. Оно должно автоматически появиться во втором окне без обновления страницы
4. В консоли браузера должны быть логи: `🔔 Realtime dishes update:`
5. В верхней панели должен быть зеленый индикатор подключения (иконка Wi-Fi)

## Примечания:

- Realtime работает только для таблиц, которые явно добавлены в публикацию
- Фильтрация по `couple_id` происходит на уровне клиента через параметр `filter` в подписке
- Если Realtime не включен, приложение будет работать, но без синхронизации в реальном времени

